export const MINIO_CLIENT = '__minioClient__'

// 默认分组
export const BUCKET_PREFIX = 'ms'
export const NO_GROUP_BUCKET = `${BUCKET_PREFIX}-nogroup`
export const OTHERS_BUCKET = 'others'
export const NEED_RECOGNITION_BUCKET = `${BUCKET_PREFIX}-needrecognition`
export const OTHER_BUCKET = `${BUCKET_PREFIX}-other`

// 分桶路径
export const SOURCE_DIR = 'source' // 源路径
export const MIN_DIR = 'min' // 压缩路径
export const THUMB_DIR = 'thumb' // 缩略图路径

// 缓存key
export const CACHE_BUCKETS = 'buckets_cache'
